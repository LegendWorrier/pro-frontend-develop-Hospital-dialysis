export namespace Lang {
    // user business logic to ensure this value before calling server
    // ( on refreshToken / credential related / any that need to pass lang value)
    export let currentLang: string;
}