import { ListDataDirective } from './list-data.directive';

describe('ListDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ListDataDirective();
    expect(directive).toBeTruthy();
  });
});
