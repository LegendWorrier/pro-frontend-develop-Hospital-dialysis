import { FilterListDirective } from './filter-list.directive';

describe('FilterListDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterListDirective();
    expect(directive).toBeTruthy();
  });
});
