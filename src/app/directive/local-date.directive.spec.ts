import { LocalDateDirective } from './local-date.directive';

describe('LocalDateDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalDateDirective();
    expect(directive).toBeTruthy();
  });
});
